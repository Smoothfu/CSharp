<Window x:Class="WpfApp36.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp36"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel Margin="10">
        <DockPanel DockPanel.Dock="Top">
            <Button Name="btnDoSync" Click="btnDoSync_Click" DockPanel.Dock="Left" HorizontalAlignment="Left"
                    >Synchronous(Same thread)</Button>
            <Button Name="btnDoAsync" Click="btnDoAsync_Click" DockPanel.Dock="Right" HorizontalAlignment="Right"
                    >Async(worker thread)</Button>
        </DockPanel>
        <ProgressBar DockPanel.Dock="Bottom" Height="18"  Name="pbProgress"/>
        <ListBox Name="lbResults" Margin="0,10"/>
    </DockPanel>
</Window>
