<Window x:Class="WpfApp32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:WpfApp32" 
        xmlns:localConverter="clr-namespace:WpfApp32.Converters"
        mc:Ignorable="d"
        Title="String Format Sample" Height="350" Width="525" Name="wnd">
    <Window.Resources>
        <local:NumbertoEvenOddConverter x:Key="evenOddConverter"/>
        <localConverter:NumberToBooleanConverter x:Key="numberToBooleanConverter"/>
    </Window.Resources>
    <StackPanel Margin="10">
        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now},ConverterCulture='de-DE',StringFormat=German date:{0:D}}"/>
        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now},ConverterCulture='en-US',StringFormat=American date:{0:D}}"/>
        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now},ConverterCulture='ja-JP',StringFormat=Japanese date:{0:D}}"/>
    </StackPanel>   
</Window>
